# Active when the app runs from docker-compose (SPRING_PROFILES_ACTIVE=docker).
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/gamedb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: gamedb
    password: gamedb
